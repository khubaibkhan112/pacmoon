import Vue from 'vue';

// Import your Vue components
import PointsIndex from './components/points/PointIndex.vue';
// import PointsModel from './components/points/PointModel.vue';

// Register the components globally
Vue.component('points-index', PointsIndex);
// Vue.component('points-model', PointsModel);

// Export Vue instance if needed
// export default Vue;
